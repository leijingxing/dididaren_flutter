# 📝 Dididaren (滴滴达人) - 产品设计与开发指南

## 1. 项目愿景 (Vision)
打造一个专注于**同城即时体力服务**的对接平台（类似“棒棒军”的数字化版本）。用户可以发布搬运、跑腿等需求，附近的劳动者（达人）接单服务。

**当前阶段目标 (Phase 1)**：
- 完成 App 核心 UI 流程。
- 实现雇主端与达人端的角色切换。
- 使用 Mock 数据模拟订单流转。
- **暂不接入**：真实地图 SDK、真实定位、后端 API。

## 2. 技术栈 (Tech Stack)
- **Framework**: Flutter
- **Language**: Dart
- **State Management**: **Riverpod** (使用 Code Generation 推荐方式)
- **Routing**: **auto_router**
- **Data Source**: 本地 Mock Data (模拟 JSON/Repository)
- **UI Style**: Material 3 Design

## 3. 用户角色与核心流程

### A. 雇主端 (Client)
1.  **首页**: 展示虚拟地图背景（Placeholder），显示“附近的达人”列表（Mock）。
2.  **下单**: 填写需求表单（类型、重量、起点终点文字描述、价格）。
3.  **订单状态**: 等待接单 -> 进行中 -> 已完成 -> 评价。

### B. 达人端 (Worker)
1.  **接单大厅**: 列表展示附近的订单（Mock数据流）。
2.  **听单/抢单**: 模拟接收订单，点击“抢单”。
3.  **执行**: 模拟执行流程（确认到达 -> 开始工作 -> 完成工作）。

## 4. 目录结构规划 (Riverpod Feature-First 架构)

```text
lib/
├── main.dart                 # App 入口
├── app_router.dart           # AutoRouter 路由配置
├── app_router.gr.dart        # 生成的路由文件
├── core/                     # 核心工具
│   ├── theme/                # 主题配置
│   ├── constants/            # 常量 (Mock Data 可放在这里)
│   └── widgets/              # 全局通用组件
├── features/                 # 功能模块
│   ├── auth/                 # 认证模块 (登录/角色选择)
│   │   ├── presentation/
│   │   └── data/
│   ├── home/                 # 首页模块 (雇主/达人 视图切换)
│   │   ├── presentation/
│   │   └── domain/
│   ├── order/                # 订单模块 (下单、接单、订单详情)
│   │   ├── data/             # Mock Repository
│   │   ├── domain/           # Order Model
│   │   ├── presentation/     # Order Widgets & Pages
│   │   └── application/      # Order Service (Riverpod Providers)
│   └── user/                 # 用户个人中心
└── shared/                   # 共享逻辑
```

## 5. 开发计划 (Roadmap)

### Step 1: 基础设施搭建
- 配置 `pubspec.yaml` (Riverpod, AutoRouter, Freezed, etc.)。
- 建立目录结构。
- 配置路由 `AppRouter`。

### Step 2: 核心 UI - 认证与首页
- **登录页**: 简单的模拟登录，选择“我是雇主”或“我是达人”。
- **首页**:
    - 雇主视角：显示“发起需求”按钮和模拟的地图背景。
    - 达人视角：显示“接单大厅”列表。

### Step 3: 业务流程 - 订单流
- **下单页**: 简单的表单（类型选择、金额输入）。
- **订单列表**: 使用 Riverpod 提供的 Mock Stream 或 List 展示订单。
- **状态流转**: 模拟从“待接单”变更为“已完成”的 UI 变化。

---
*Created by Gemini CLI*
